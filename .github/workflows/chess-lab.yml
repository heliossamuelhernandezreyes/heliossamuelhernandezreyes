name: Chess Lab — Stockfish

on:
  push:
    branches: [chess-lab]
    paths:
      - 'chess-lab/games/**/*.pgn'
      - 'chess-lab/analyze.py'
      - 'chess-lab/requirements.txt'
      - '.github/workflows/chess-lab.yml'
  workflow_dispatch:
    inputs:
      pgn:
        description: 'PGN path, e.g. chess-lab/games/current.pgn'
        required: true
        default: 'chess-lab/games/2026-08-28-smith-morra.pgn'
      depth:
        description: 'Stockfish depth'
        required: false
        default: '20'

permissions:
  contents: write

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: chess-lab
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install Stockfish and Python dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y stockfish
          pip install -r chess-lab/requirements.txt
      - name: Select PGN
        id: select
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            PGN="${{ inputs.pgn }}"
            DEPTH="${{ inputs.depth }}"
          else
            PGN=$(find chess-lab/games -name '*.pgn' -type f | sort | tail -n 1)
            DEPTH=20
          fi
          test -n "$PGN"
          test -f "$PGN"
          echo "pgn=$PGN" >> "$GITHUB_OUTPUT"
          echo "depth=$DEPTH" >> "$GITHUB_OUTPUT"
      - name: Analyze with Stockfish
        env:
          STOCKFISH_PATH: /usr/games/stockfish
        run: |
          python chess-lab/analyze.py "${{ steps.select.outputs.pgn }}" \
            --depth "${{ steps.select.outputs.depth }}" \
            --multipv 3 \
            --outdir chess-lab/analysis
      - name: Commit analysis
        run: |
          git config user.name 'chess-lab-bot'
          git config user.email 'chess-lab-bot@users.noreply.github.com'
          git add chess-lab/analysis
          if git diff --cached --quiet; then
            echo 'No analysis changes.'
          else
            git commit -m 'chess-lab: analyze PGN with Stockfish'
            git pull --rebase origin chess-lab
            git push origin HEAD:chess-lab
          fi
